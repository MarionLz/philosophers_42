Action philosophes :

- creation du thread : "THINK";
- 
- verouillage fourchettes si dispo, en attente sinon;
- si une fourchette prise, message "timestamp_in_ms X has taken a fork";
- des que les deux fourchettes sont prises, EAT :
		- remettre le compteur time_to_die a 0;
		- message "timestamp_in_ms X is eating";
		- usleep (time_to_eat);
- repose les fourchettes (unlock mutex);
- commence a dormir :
		- "timestamp_in_ms X is sleeping";
		- usleep(time_to_sleep);


fontion get status avec mutex qui bloque la variable, la debloque et renvoie la valeur
idem pour set pour metttre a jour la variable
